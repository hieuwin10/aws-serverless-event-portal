{
  "name": "event-app-backend",
  "version": "1.0.0",
  "description": "AWS Lambda Serverless Backend for Event Management App",
  "main": "dist/localServer.js",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/localServer.ts",
    "start": "node dist/localServer.js",
    "start:local": "ts-node src/localServer.ts",
    "deploy": "sam build && sam deploy",
    "deploy:guided": "sam build && sam deploy --guided",
    "logs": "sam logs -n GetEventsFunction --stack-name eventapp --tail",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.500.0",
    "@aws-sdk/lib-dynamodb": "^3.500.0",
    "dotenv": "^17.3.1",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.130",
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "@types/uuid": "^9.0.8",
    "express": "^4.19.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
